pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,vue,json,jsonc,css,html}"
      run: npx @biomejs/biome check --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}

pre-push:
  commands:
    typecheck:
      run: npm run typecheck
